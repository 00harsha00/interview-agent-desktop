# Dev mode uses production backend (Fly.io) so Answer works without local backend.
# To use local backend: change to http://localhost:3000
# and run: cd ~/Desktop/parakeet-backend && npm run dev
VITE_API_BASE_URL=https://interview-agent-backend-vnrl-a.fly.dev
VITE_BACKEND_URL=https://interview-agent-backend-vnrl-a.fly.dev
VITE_FRONTEND_URL=http://localhost:4000
