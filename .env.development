# Development — local backend/frontend.
# NOTE: .env files have no "||" fallback syntax — exactly one value per var.
# The ngrok URL for distribution builds goes in .env.production instead.
VITE_API_BASE_URL=http://localhost:3000
VITE_FRONTEND_URL=http://localhost:4000
